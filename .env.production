# Build de producao (vite build): carrega 150 produtos + vendas de demonstracao no painel.
# Para usar apenas dados reais do Postgres, defina no host (ex. Render): VITE_DEMO_DATA=false
VITE_DEMO_DATA=true
